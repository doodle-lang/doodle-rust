# A tiny interactive program: ask the user's name, then greet them.
#
# `read_line` reads a line from the terminal; string interpolation ({name}) builds the reply.
#
#   doodle run examples/gallery/greeting.doodle

print("What's your name?")
let name = read_line()
print("Nice to meet you, {name}!")
